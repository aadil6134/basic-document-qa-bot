# Source Documents

Place assignment documents here before running ingestion. The bot supports:

- PDF files (`.pdf`)
- Word documents (`.docx`)
- Plain text files (`.txt`)

The included TXT file is only a small smoke-test document so you can verify the pipeline before adding real documents.
